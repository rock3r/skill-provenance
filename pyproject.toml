[project]
name = "skill-provenance"
version = "0.1.0"
description = "Portable agent skill for tracking skill source metadata"
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"

[tool.pytest.ini_options]
python_files = ["test_*.py"]
